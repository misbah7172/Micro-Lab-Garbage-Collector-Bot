# Core computer vision and AI packages
opencv-python==4.8.1.78
ultralytics==8.0.196
supervision==0.16.0

# Serial communication
pyserial==3.5

# Image processing and utilities
numpy<2  # Use NumPy 1.x for compatibility with OpenCV and other packages
pillow==10.0.0

# PyTorch (required by YOLO)
torch>=2.0.0
torchvision>=0.15.0

# Web dashboard dependencies
flask==3.0.0
flask-socketio==5.3.6
python-socketio==5.9.0
python-engineio==4.7.1
python-dotenv==1.0.0

# Dashboard and system utilities
matplotlib==3.7.2
requests==2.31.0

# Additional standard library modules (included with Python)
# threading - For background serial data reading
# re - For parsing sensor data from ESP32
# datetime - For timestamps and collection logging
# time - For timing operations and heartbeat
# sys - For system operations and exit handling
# os - For file system operations

# Development and debugging tools
colorama==0.4.6  # For colored console output (optional)

# Optional: For enhanced YOLO features
# onnx==1.14.1
# onnxruntime==1.15.1

# Note: Install these packages using:
# pip install -r requirements.txt
#
# For GPU acceleration (optional):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For development with better console output:
# pip install colorama